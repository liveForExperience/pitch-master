{
  "name": "pitchmaster",
  "version": "0.0.0",
  "private": true,
  "description": "PitchMaster v2 monorepo root (npm workspaces: backend + web).",
  "workspaces": [
    "backend",
    "web"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "bash bin/dev.sh",
    "dev:backend": "npm run dev -w pitchmaster-backend",
    "dev:web": "npm run dev -w pitchmaster-web",
    "build": "npm run build -w pitchmaster-backend && npm run build -w pitchmaster-web",
    "typecheck": "npm run typecheck -w pitchmaster-backend && npm run typecheck -w pitchmaster-web",
    "test": "npm run test -w pitchmaster-backend && npm run test -w pitchmaster-web"
  }
}
